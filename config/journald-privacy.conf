# MerphisOS - systemd-journald privacy
[Journal]
# Don't persist logs beyond current boot
Storage=volatile
# Limit log size
SystemMaxUse=50M
# Don't forward to /dev/console
ForwardToConsole=no
# Don't log audit messages
Audit=no
