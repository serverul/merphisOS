# MerphisOS - VLC privacy: disable all telemetry and updates
[main]
# Disable update checking
update-check=false
# Disable update channel
update-channel=none
# Disable media usage statistics
metadata-network-access=0
# Disable lua network
lua-network=0
